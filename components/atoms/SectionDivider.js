import React from 'react';
import {View} from 'react-native';

const SectionDivider = () => {
  return (
    <View style={{height: 60, width: '100%', backgroundColor: '#979797'}} />
  );
};
export default SectionDivider;
